# Vendored font sources

- Inter 4.1 static TTFs: the `rsms/inter` 4.1 release.
- JetBrains Mono static TTFs: the same vendored files as
  `TheAxiomFoundation/axiom-quarto`.

Each family directory contains its SIL Open Font License beside the binaries.
